server:
  port: 8080
  servlet:
    context-path: /store

logging:
  level:
    root: DEBUG

--- # production
spring:
  profiles: prod
  #application:
    #basePath: /
  jpa:
    hibernate.ddl-auto: create
    show-sql: true
    properties.hibernate.format_sql: true
  datasource:
    url: jdbc:mariadb://10.178.230.98:3306/depd
    username: admin
    password: EvzdKZX1XR
logging:
  level:
    root: INFO
redis:
  uri: redis://O6L6ATk7Be@10.178.230.75:6379

--- # local
spring:
  profiles: local
  application:
    basePath: /swagger-ui.html
  jpa:
    hibernate.ddl-auto: create
    show-sql: true
    properties.hibernate.format_sql: true
  datasource:
    url: jdbc:mariadb://localhost:3306/depd
    username: root
    password: root
logging:
  level:
    root: DEBUG
redis:
  uri: redis://localhost:6379/

